# CONFIGURACIÓN MONGODB ATLAS
PORT=5000
NODE_ENV=development

# MongoDB Atlas - Reemplazar con datos propios
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority&appName=YourAppName

# JWT Secret - Generar uno propio (no usar este ejemplo)
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure_minimum_32_chars

# Frontend
FRONTEND_URL=http://localhost:5173

# Instrucciones:
# 1. Renombrar este archivo a .env
# 2. Reemplazar los valores con tus propias credenciales
# 3. Nunca compartir el archivo .env real
# 4. Mantener este archivo .env.example actualizado con la estructura pero sin datos sensibles